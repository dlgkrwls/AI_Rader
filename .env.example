# Copy to .env and fill in. .env is gitignored.
# Create at https://github.com/settings/tokens - a classic token with no scopes
# (or a fine-grained token with public read access) is enough for search.
GITHUB_TOKEN=