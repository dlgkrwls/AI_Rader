# Personal AI Radar

개인 관심사 기반 AI 기술 인텔리전스 시스템.
논문·오픈소스·모델·빅테크 발표를 매일 자동 수집하고, 개인 관심도와 성장 추세로
랭킹해 "오늘 볼 만한 10개"만 남긴다. 축적 데이터는 RAG 지식베이스가 된다.

**이 프로젝트는 취업 포트폴리오다.** 따라서 코드가 동작하는 것만으로는 부족하고,
사용자가 모든 설계 결정을 직접 설명할 수 있어야 한다. 이 제약이 아래 규칙의 근거다.

---

## 현재 상태

- Phase: **1 (수집 파이프라인)**
- 완료: **T1 T2 T3 T4 T5 T8** (+ T5 후속: HF 정렬을 downloads로 교체)
- 남음: **T6 (블로그 수집기)**, **T7 (오케스트레이션 마무리)**, **T9 (대시보드)**
- 다음 태스크: **T6.** 예측은 `docs/DECISIONS.md`에 이미 적혀 있다.
- 상세: `docs/PHASE1.md`

> 태스크가 끝날 때마다 이 섹션을 갱신할 것.

### 지금 돌아가고 있는 것

- **스케줄러 등록 완료.** 매일 08:00에 `scripts/collect.py`가 자동 실행된다
  (작업 이름 `AI_Radar_Collect`, 등록 방법은 `docs/SCHEDULER.md`).
  T8의 남은 확인 하나: **다음 날 아침 `logs/collect_YYYY-MM-DD.log`가 생겼는지.**
- 실행 환경은 conda **`radar`** (Python 3.11.16).
  절대 경로: `C:\Users\user\anaconda3\envs\radar\python.exe`
  base 아나콘다(3.12)에는 `feedparser`가 없어 수집기가 안 돈다.
- 테스트: `python -m unittest discover -s tests` (pytest 안 씀 — 표준 라이브러리 우선)
- DB 누적 686건 (arxiv 339 / github 148 / huggingface 199), `item_metrics` 2,204행.
  `data/radar.db`는 gitignore 대상이라 **이 PC에만 있다.** 따로 백업할 것.
- `.env`에 `GITHUB_TOKEN` 있음 (gitignore 대상). HF와 arXiv는 인증 불필요.

### T7에 남은 일

`scripts/collect.py`는 T8을 먼저 하기 위해 **최소한으로만** 만들어 둔 상태다 (56줄).
예외 격리와 파일 로깅은 이미 들어 있고, 남은 것은 요약 출력(소스별 건수 · 소요 시간)과
수집기 생성 자체가 실패했을 때 `collection_runs`에 기록을 남기는 것이다.
**경로는 바꾸지 말 것** — 스케줄러 작업이 이 경로를 하드코딩하고 있다.

---

## 작업 규칙

1. **한 번에 태스크 하나만.** T1이 끝나면 멈춘다. T2로 자동 진행하지 않는다.
2. 태스크 시작 전, `docs/PHASE1.md`에서 해당 태스크의 **완료 조건**을 확인한다.
3. 태스크 완료 시 다음 순서를 지킨다.
   - 완료 조건 충족을 실제로 검증 (실행해서 확인, 추측 금지)
   - `docs/DECISIONS.md`에 결정 사항 추가 (양식은 파일 상단 참조)
   - 커밋
   - 사용자에게 **무엇을 왜 그렇게 했는지 3~5줄로 설명**
4. **diff가 150줄을 넘을 것 같으면 먼저 알린다.** 태스크를 쪼갤지 사용자가 판단한다.
5. 코드를 쓰기 전에 접근 방식을 2~3줄로 먼저 말한다. 긴 설계 문서는 쓰지 않는다.
6. **`docs/DECISIONS.md`는 사용자와 에이전트가 함께 편집한다.** 에이전트는 append만
   하지만, 사용자의 편집기 버퍼가 낡은 상태로 저장되면 에이전트의 기록이 지워진다.
   실제로 두 번 위험했다. 그래서:
   - 사용자는 예측을 쓰기 **전에** 편집기에서 파일을 새로 읽는다
     (VS Code: `File > Revert File`).
   - 에이전트는 append 전에 파일이 온전한지 확인하고, 사용자가 붙여넣은 내용이
     디스크와 다르면 **덮어쓰지 말고 차이를 먼저 알린다.**

---

## 절대 규칙 (어기면 데이터를 잃는다)

- **`raw_json`은 항상 저장한다.** API 응답 원본 전체. 나중에 필드가 더 필요해질 때
  과거 데이터를 복원할 유일한 수단이다.
- **변하는 값은 `items`에 넣지 않는다.** stars / forks / downloads / likes는
  `item_metrics`에 append-only로 쌓는다. `items`에 넣고 UPDATE하면 트렌드 분석의
  근거가 영구히 사라진다.
- **스키마 변경은 사용자 승인 후에만.** 컬럼 추가·삭제·타입 변경 모두 해당.
  먼저 제안하고 이유를 설명한 뒤 승인을 기다린다.
- **수집기 하나의 실패가 전체 파이프라인을 중단시키면 안 된다.** 예외는 소스 단위로
  격리하고, 실패는 `collection_runs`에 기록한 뒤 다음 소스로 진행한다.

---

## 금지 목록

이 프로젝트의 규모(테이블 3개, 사용자 1명, 하루 수백 건)에서는 아래가 전부 과잉이다.
필요해 보이면 도입하지 말고 **먼저 사용자에게 이유를 설명하고 물어본다.**

- `asyncio` / 동시성 — 순차 처리로 충분하다. 수집 시간은 병목이 아니다.
- ORM (SQLAlchemy 등) — `sqlite3` 표준 라이브러리를 쓴다. SQL을 직접 쓰는 편이
  스키마 이해에 유리하고, 이 프로젝트에서는 그게 목적이다.
- 커스텀 예외 계층 — 표준 예외로 충분하다.
- 추상 클래스 / 인터페이스 — **구현체를 2개 만든 뒤에** 공통점을 추출한다.
  구현 없이 먼저 추상화하면 거의 항상 틀린 추상화가 나온다. (T3이 이 순서를 따른다)
- LangChain / LlamaIndex 계열 — RAG의 각 단계를 직접 구현하는 것이 목적이다.
- 설정 클래스, 의존성 주입 컨테이너, 플러그인 시스템 — 전부 불필요.
- 방어적 코드 남발 — 일어나지 않을 상황에 대한 분기는 넣지 않는다.

---

## 코딩 스타일

- Python 3.11, 표준 라이브러리 우선
- 코드 주석 · 커밋 메시지 · 변수명: **영어**
- 문서 (`docs/`, `DECISIONS.md`) · 사용자 대화: **한국어**
- 커밋: Conventional Commits (`feat:`, `fix:`, `chore:`, `docs:`, `test:`)
- 함수는 짧게. 한 함수가 화면을 넘어가면 쪼갠다.
- 타입 힌트는 함수 시그니처에만. 내부 변수까지 붙이지 않는다.
- 로깅은 `logging` 표준 모듈, 파일 출력 필수 (`logs/`).
  자동 실행 시 콘솔 출력은 아무도 보지 않는다.

---

## 테스트 정책 (Phase 1)

- **각 수집기의 `parse()` 함수만** 테스트한다. 고정된 샘플 응답(fixture)을 입력으로
  기대하는 구조가 나오는지 확인.
- 네트워크 호출은 테스트하지 않는다. 느리고 불안정하며 얻는 게 적다.
- 커버리지 목표 없음. 파싱 로직이 API 응답 변경에 깨지는 것이 실제로 일어나는 사고이고,
  나머지는 지금 단계에서 비용만 크다.

---

## 사용자와의 협업 방식

사용자는 이 코드를 **면접에서 설명해야 한다.** 따라서:

- 설명 없이 코드만 던지지 않는다.
- 사용자가 예측하지 못했을 만한 부분(왜 이 처리가 필요한지)은 특히 짚어준다.
- 사용자가 잘못된 방향을 요청하면 **그대로 따르지 말고 지적한다.**
  근거를 대고, 그래도 원하면 따른다.
- "이렇게 하면 더 좋습니다"라며 범위를 넓히지 않는다. 범위 확장이 이 프로젝트의
  가장 큰 실패 요인이다.

---

## 참조 문서

| 파일 | 내용 |
|---|---|
| `docs/ARCHITECTURE.md` | 파이프라인 구조, 왜 이 순서인지 |
| `docs/SCHEMA.md` | 테이블 정의와 설계 결정 3가지 |
| `docs/PHASE1.md` | T1~T9 태스크와 완료 조건 |
| `docs/DECISIONS.md` | 구현하며 내린 결정 기록 (append-only) |
| `config/sources.yaml` | 수집 소스 목록 |
| `config/profile.yaml` | 관심 키워드 (Phase 2 랭킹 입력) |
